<%= form_with(model: moderador, local: true) do |form| %>
  <% if moderador.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(moderador.errors.count, "error") %> prohibited this moderador from being saved:</h2>

      <ul>
      <% moderador.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :senha %>
    <%= form.text_field :senha %>
  </div>

  <div class="field">
    <%= form.label :sal %>
    <%= form.number_field :sal %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
